diff --git a/src/impl/tlstransport.hpp b/src/impl/tlstransport.hpp
index 2322b86..b1b0b7e 100644
--- a/src/impl/tlstransport.hpp
+++ b/src/impl/tlstransport.hpp
@@ -35,6 +35,10 @@ public:
 	bool send(message_ptr message) override;
 
 	bool isClient() const { return mIsClient; }
+	// GCC 7.x sometimes instantiates the generic std::visit lambda first,
+	// which expects isActive() to exist on all alternatives.
+	bool isActive() const { return mIsClient; }
 
 protected:
